n = notepad $*
master = git checkout master
cb = git checkout dev/%__GitUserName%/$*
nb = git checkout -b dev/%__GitUserName%/$*
pull = git pull
branch = git branch
stat = git status
mmerge = git checkout master && git merge dev/%__GitUserName%/$* --squash && git commit
